# Allows the cast-API `limit` param: NULL (no limit), -1 (no limit), or a positive integer

Allows the cast-API `limit` param: NULL (no limit), -1 (no limit), or a
positive integer

## Usage

``` r
assert_limit_param(value)
```

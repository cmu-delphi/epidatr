# Allows a report-time-family param (a `report_time` comparison bound or `snapshot_date`): date, character, integer-like, or `POSIXt`

Allows a report-time-family param (a `report_time` comparison bound or
`snapshot_date`): date, character, integer-like, or `POSIXt`

## Usage

``` r
assert_report_time_param(name, value, len = NULL, required = TRUE)
```

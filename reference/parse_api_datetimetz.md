# Parses a cast-API UTC timestamp string (e.g. "2025-10-16T13:45:00Z") to a `POSIXct`

Parses a cast-API UTC timestamp string (e.g. "2025-10-16T13:45:00Z") to
a `POSIXct`

## Usage

``` r
parse_api_datetimetz(value)
```

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/endpoints.R
\name{epidata_meta}
\alias{epidata_meta}
\title{Get cast-API source metadata}
\usage{
epidata_meta(source = NULL, fetch_args = fetch_args_list())
}
\arguments{
\item{source}{string. The data source to query. If \code{NULL} (default), returns
metadata for all available sources. If specified, returns metadata for
the given source.}

\item{fetch_args}{\code{\link[=fetch_args_list]{fetch_args_list()}}. Additional arguments to pass
to \code{fetch()}. See \code{fetch_args_list()} for details.}
}
\value{
list. If \code{source} is \code{NULL}, a named list of source metadata objects.
If \code{source} is specified, the metadata list for that source.
}
\description{
\code{epidata_meta} returns source-level metadata from the cast-API,
including \code{report_time} ranges, \code{reference_time} ranges, and lists of
available signals and geo types.
}
\section{See also}{

For example queries showing how to discover signals and build calls,
see \code{vignette("signal-discovery", package = "epidatr")}.
}

\seealso{
\code{\link[=epidata_snapshot]{epidata_snapshot()}}, \code{\link[=epidata_archive]{epidata_archive()}}, \code{\link[=epidata]{epidata()}}, \code{\link[=epirange]{epirange()}}
}
\keyword{endpoint}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/endpoints.R
\name{pub_cast}
\alias{pub_cast}
\title{Various signals via cast-API}
\usage{
pub_cast(
  source,
  signals,
  geo_type,
  geo_values = "*",
  time_values = "*",
  ...,
  as_of = NULL,
  version = NULL,
  fetch_args = fetch_args_list()
)
}
\arguments{
\item{source}{string. The data source to query (see:
\url{https://cmu-delphi.github.io/delphi-epidata/api/covidcast_signals.html}).}

\item{signals}{character vector. The signals to query.}

\item{geo_type}{string. The geographic resolution of the data (see:
\url{https://cmu-delphi.github.io/delphi-epidata/api/covidcast_geography.html}).}

\item{geo_values}{character. The geographies to return. Defaults to all
("*") geographies within requested geographic resolution (see:
\url{https://cmu-delphi.github.io/delphi-epidata/api/covidcast_geography.html}.).}

\item{time_values}{\code{\link{timeset}}. Dates or epiweeks to fetch.
Supports \code{\link[=epirange]{epirange()}} and defaults to all ("*") dates.}

\item{...}{not used for values, forces later arguments to bind by name}

\item{as_of}{Date. Optionally, the as-of date for the issues to fetch.
See the "Data Versioning" section for details. Internally maps to the
\code{snapshot_date} parameter. Defaults to NULL (latest available).}

\item{version}{Date, string, or \code{\link[=epirange]{epirange()}}. A version query for the
archive endpoint. Supports exact dates (e.g., "2025-10-16"),
operators (e.g., "<2025-10-16"), or an \code{\link[=epirange]{epirange()}} (the end of the
range is used as the upper bound, e.g., \verb{<2024-01-05}). Internally
maps to the \code{version_query} parameter.}

\item{fetch_args}{\code{\link[=fetch_args_list]{fetch_args_list()}}. Additional arguments to pass
to \code{fetch()}. See \code{fetch_args_list()} for details.}
}
\value{
\code{\link[tibble:tibble]{tibble::tibble}}
}
\description{
\code{pub_cast} provides access to the cast-API, supporting snapshot and
archive queries for multiple signals simultaneously.
}
\seealso{
\code{\link[=pub_cast_meta]{pub_cast_meta()}}, \code{\link[=epirange]{epirange()}}
}
\keyword{endpoint}

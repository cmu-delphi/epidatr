% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/request.R
\name{perform_and_read}
\alias{perform_and_read}
\title{Perform a request, deciding whether to hold the body in memory or on disk.}
\usage{
perform_and_read(req, stream_threshold_bytes = Inf, download_path = NULL)
}
\arguments{
\item{req}{an \code{httr2} request object}

\item{stream_threshold_bytes}{see \code{\link[=do_request]{do_request()}}}

\item{download_path}{see \code{\link[=do_request]{do_request()}}}
}
\value{
an \code{httr2_response}
}
\description{
\code{\link[httr2:req_perform_connection]{httr2::req_perform_connection()}} returns the headers with the
body still an open connection, so we can read \code{Content-Length} before pulling
the body. Small bodies and all error bodies are read into memory. Larger
ones or bodies with no \code{Content-Length} are streamed to a temp file a chunk
at a time to keep peak memory low.
}
\keyword{internal}

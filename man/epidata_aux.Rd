% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/endpoints.R
\name{epidata_aux}
\alias{epidata_aux}
\alias{epidata_aux.default}
\alias{epidata_aux.data.frame}
\title{Fetch V5 auxiliary data}
\usage{
epidata_aux(source, ...)

\method{epidata_aux}{default}(
  source,
  ...,
  reference_time = "*",
  time_values = lifecycle::deprecated(),
  snapshot_date = NULL,
  report_time = "*",
  issues = lifecycle::deprecated(),
  columns = NULL,
  fetch_args = fetch_args_list()
)

\method{epidata_aux}{data.frame}(source, ..., columns = NULL, fetch_args = fetch_args_list())
}
\arguments{
\item{source}{A source string to retrieve auxiliary data directly, or a tibble returned by
\code{\link[=epidata_snapshot]{epidata_snapshot()}} or \code{\link[=epidata_archive]{epidata_archive()}} to merge the data onto (its
source is recovered automatically).}

\item{...}{Named filters on the auxiliary key columns, such as
\code{pcr_target = "sars-cov-2"} or \code{geo_value = c("ca", "ny")}. Each key accepts
one or more values (matched as OR); they are serialized as repeated
\code{key:value} terms server-side to keep the aux pull small. Passing more than
10 values for a key warns, since the request URL may get too long. When
\code{source} is a tibble and no filters are given, they are inferred from the
base: each key it narrows to at most 10 distinct values is filtered to those.}

\item{reference_time}{\code{\link{timeset}}. Reference time to return (filters on the
\code{reference_time} column). Supports individual dates or \code{\link[=epirange]{epirange()}}.
Only used when \code{source} is a string.}

\item{time_values}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Use \code{reference_time} instead.}

\item{snapshot_date}{Date, \code{POSIXt}, UTC timestamp string (e.g.
\code{"2025-10-16T13:45:00Z"}), \code{"latest"}, or \code{NULL}. Return auxiliary data
as it appeared at this date or instant (one row per key, the most recent
version active then). \code{"latest"} uses today's date. Use \code{NULL} (default)
to return the full version history filtered by \code{report_time}. Mutually
exclusive with \code{report_time}. Only used when \code{source} is a string.}

\item{report_time}{String or \code{\link[=epirange]{epirange()}} specifying the version of the
auxiliary data to retrieve. Accepts comparison operators (e.g.,
\code{"<2025-10-16"}, \code{">=2025-10-16"}, or \code{"<=2025-10-16T13:45:00Z"} for a
UTC timestamp bound) or an \code{\link[=epirange]{epirange()}} for an inclusive date range.
Bare dates and the \code{"="} operator are not accepted: use
\code{snapshot_date} for point-in-time data. Mutually exclusive with
\code{snapshot_date}. Only used when \code{source} is a string.}

\item{issues}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Use \code{report_time} instead.}

\item{columns}{A character vector of columns to return. By default, all columns are returned.}

\item{fetch_args}{\code{\link[=fetch_args_list]{fetch_args_list()}}. Additional arguments to pass
to \code{fetch()}. See \code{fetch_args_list()} for details.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble}}.
}
\description{
Fetch auxiliary data associated with a cast signal.

You can pass a source string to fetch the auxiliary data directly. Alternatively,
you can pass the output of \code{\link[=epidata_snapshot]{epidata_snapshot()}} or \code{\link[=epidata_archive]{epidata_archive()}}. In this
case, \code{epidata_aux} automatically retrieves the source from the object, fetches
the matching auxiliary data, and performs a version-aware left join onto the base data.

For the auxiliary key columns and their allowed values, see the source's API
docs, e.g. NWSS:
\url{https://cmu-delphi.github.io/delphi-epidata/api/v5-signals/nwss.html}.
}
\seealso{
\code{\link[=epidata_snapshot]{epidata_snapshot()}}, \code{\link[=epidata_archive]{epidata_archive()}}, \code{\link[=epidata_meta]{epidata_meta()}}
}
\keyword{endpoint}

# Fetch the declared aux key columns for a source from the cast-API `metadata/aux_schema/` endpoint.

Fetch the declared aux key columns for a source from the cast-API
`metadata/aux_schema/` endpoint.

## Usage

``` r
.aux_key_columns(source, fetch_args)
```
